import createCalibrationService from './createCalibrationService';

const micrometerCalibrationService = createCalibrationService(
  'micrometer-calibrations'
);

export default micrometerCalibrationService;
